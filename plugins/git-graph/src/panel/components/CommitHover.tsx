import type { GraphCommit, Translate } from '../domain/index.js';
export function CommitHover({ commit, onCopy, onOpen, t }: { commit: GraphCommit; onCopy?(hash: string): void; onOpen?(): void; t: Translate }) { const locale = typeof document === 'undefined' ? undefined : document.documentElement.lang; return <article className="git-commit-hover" data-git-graph-commit-hover="true"><strong>{commit.subject}</strong><p>{commit.author} · {new Date(commit.timestamp).toLocaleString(locale)}</p><code>{commit.id}</code><button type="button" aria-label={t('hover.copyHash')} onClick={() => onCopy?.(commit.id)}>{t('hover.copyHash')}</button>{onOpen && <button type="button" aria-label={t('hover.openGitGraph')} onClick={onOpen}>{t('hover.openGitGraph')}</button>}</article>; }
