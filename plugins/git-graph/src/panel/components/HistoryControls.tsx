import type { HistoryMode, Translate } from '../domain/index.js';
export function HistoryControls({ mode, manualRefs, t, onMode, onManualRefs, onApply }: { mode: HistoryMode; manualRefs: string; t: Translate; onMode(mode: HistoryMode): void; onManualRefs(value: string): void; onApply(): void }) { return <fieldset className="git-history-controls"><legend>{t('workspace.history')}</legend>{(['auto', 'all', 'manual'] as const).map((item) => <label key={item}><input type="radio" name="git-history-mode" checked={mode === item} onChange={() => onMode(item)} />{t(`workspace.history${item[0]!.toUpperCase()}${item.slice(1)}`)}</label>)}{mode === 'manual' && <input value={manualRefs} onChange={(event) => onManualRefs(event.target.value)} aria-label={t('workspace.historyManualRefs')} />}<button type="button" onClick={onApply}>{t('workspace.apply')}</button></fieldset>; }
